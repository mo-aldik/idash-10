name: Deploy Github Actions - DEV

on:
  push:
    branches:
      - develop

env:
  VITE_REACT_APP_ENV_VAR: 'development'
  VITE_REACT_APP_BASE_URL: 'http://10.30.11.21:9058'
  VITE_REACT_APP_BASE_URL_2: 'http://10.30.11.21:9055'
  VITE_GOOGLE_MAPS_API_KEY: 'AIzaSyBUtZjrs_fplvEWPYljDM2e_yDwEWMpaTM'
  VITE_UAEPASS_URL_EN: 'https://stg-id.uaepass.ae/idshub/authorize?response_type=code&client_id=emirates_parking_web_stage&scope=urn:uae:digitalid:profile:general&state=sww&redirect_uri=https://auth.emiratesparkingservices.com/&acr_values=urn:safelayer:tws:policies:authentication:level:low&failureURL=https://auth.emiratesparkingservices.com/&ui_locales=en'
  VITE_UAEPASS_URL_AR: 'https://stg-id.uaepass.ae/idshub/authorize?response_type=code&client_id=emirates_parking_web_stage&scope=urn:uae:digitalid:profile:general&state=sww&redirect_uri=https://auth.emiratesparkingservices.com/&acr_values=urn:safelayer:tws:policies:authentication:level:low&failureURL=https://auth.emiratesparkingservices.com/&ui_locales=ar'
  VITE_UAEPASS_LOGOUT_URL: 'https://stg-id.uaepass.ae/idshub/logout?redirect_uri=https://auth.emiratesparkingservices.com'

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [22.x]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: |
            **/node_modules
          key: ${{ runner.os }}-${{ github.ref }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-${{ github.ref }}-
            ${{ runner.os }}-

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          CI: false
