export enum LocalStorageKeys {
  access_token = 'access-token',
}

export enum URL {
  // General
  HOME = '/',
  NOT_FOUND = '/404',
  ACCESS_DENIED = '/access-denied',

  // Pre Login
  LOGIN = '/login',
}
